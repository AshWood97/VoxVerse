# 执行摘要

VoxVerse（原 SpeakMate）当前是一款基于 Tauri 2 + Vue3 的语音角色扮演学习桌面应用，已实现角色系统、对话历史、学习反馈、生词本等核心功能。目前主干功能已接近稳定（Lv.0–Lv.3 里程碑已完成，Lv.4 主要代码已实现但仍需真实端点验证），尚无正式 v0.2 发布说明。为制定 v0.3 升级方案，本报告梳理了现有功能与模块、技术债与风险，提出了功能与架构改进建议，并给出详细里程碑计划、资源需求和兼容性说明。

<!-- 图片：架构示意图 -->
```mermaid
flowchart LR
  subgraph 前端Vue/Tauri应用
    A[App.vue 主入口] --> B[录音/语言输入界面]
    A --> C[会话 & 角色管理界面]
    A --> D[学习反馈 & 统计界面]
  end
  subgraph Tauri 后端 (Rust)
    E[IPC Command 接收] --> F[会话协调器]
    F --> G[SQLite 本地数据库]
    F --> H[LLM 调用层 (OpenAI/OpenAI-兼容、Ollama)]
    F --> I[语音服务层: STT(Tauri+Whisper)、TTS(Edge TTS/浏览器)]
    F --> J[长期/检索记忆层]
    F --> K[关系引擎 (亲密度、信任等)]
  end
  B -- 语音输入 --> I
  I -- 文本输出 --> F
  F -- 文本输出 --> D
  C -- 配置角色 --> G
  D -- 添加反馈数据 --> G
```  

# 一、v0.2 现状回顾

**主要功能：** 根据 README 和文档，当前版本（假定为 v0.2）具备以下核心能力：

- **语音与文本对话**：支持实时流式聊天（OpenAI 兼容接口）；前端录音输入，后端 Whisper/STT 转文本，调用 LLM，最后通过 Edge TTS 或浏览器合成语音输出。
- **角色系统**：可创建/编辑角色（含头像、性格等设定），角色数据存 SQLite 持久化；支持角色导入导出和预设角色。前端侧提供角色切换和配置界面。
- **会话与练习模式**：已有会话历史视图，可按不同“练习模式”（如自由对话、情景对话等）启动对话；对话消息持久化在 `messages` 表中。部分练习模式和场景功能已实现（接口预留或原型）。
- **学习反馈与生词本**：会话后端生成纠错、单词记录、总结报告等学习反馈。前端有反馈面板（`FeedbackPanel.vue`）和差异视图(`DiffView.vue`)来展示反馈结果。
- **多层记忆引擎**：设计了四层记忆体系，包括对话事件层、用户长期事实库等。前端有“认知记忆面板”(Cognitive Memory Panel)可视化事实条目，后端使用 SQLite 存储长期记忆和关系状态。
- **关系引擎**：记录角色与用户间的亲密度、信任、故事阶段、承诺和禁区等。前端提供关系卡片可调整这些值。
- **多语言界面**：支持中文和英文界面（`vue-i18n`，含基本中英文资源）。当前发现存在翻译类型不匹配等小问题。
- **原生 CLI 与 Agent**：实现了名为 `voxverse-cli`（文档中仍称 `speakmate`）的 Rust CLI，支持 JSON 输出的状态查看、会话导出、审计日志查询等。还提供简易的 JSON-LD/LLM Agent 控制接口，安全地允许写操作（需显式授权）和网络操作（需 `--allow-network`）。
- **架构概述**：项目采用前端 Vue 组件 + Tauri IPC 调用结合后端 Rust 的混合架构。前端组件按功能分为基础组件（`base/`）、业务组件（`domain/`）、状态组合（`composables/`）和 Tauri 服务封装（`services/tauri/`）。后端组织为 commands、services、repositories（数据库）和 state（运行态与密钥）。

**依赖与构建：** 前端使用 Vue3、Vite、TypeScript、Tauri 插件（Store、Opener 等）；后端 Rust 依赖 tauri 2.x、serde、reqwest、tokio、rusqlite（带 libsqlite）、aes-gcm、keyring（系统密钥存储）、msedge-tts 等。项目提供打包命令（PNPM + Tauri）和验证脚本，可构建 Windows 安装包。**测试覆盖**信息不明确，仓库中未见专门的单元测试目录或覆盖报告；根据验证脚本可知有 Rust 层测试（`cargo test`）和格式/警告检查。 

**文档与运行状况：** 除主 README 外，还有详尽的维护者文档（路线图 `task.md`、实施计划 `implementation_plan.md`、维护者指南 `walkthrough.md`、CLI 使用文档 `native-cli.md` 等）。文档大多用中文撰写，信息全面，但存在描述不一致和部分翻译未完善的问题。根据 `walkthrough.md` 的风险提示，当前代码**构建尚不绿灯**：比如 `App.vue` 缺少 i18n 导入，`i18n.ts` 类型不匹配，导致 `pnpm build` 失败。此外，部分依赖的外部服务（如 Whisper STT、Ollama）还未在真实环境中验证，存在阻塞项。

**已知问题/缺陷：** 
- **构建与兼容性问题**：前述文档加载错误需修复，项目 id（App 名）已从 SpeakMate 改为 VoxVerse，部分 Legacy 路径兼容性需要清理。目前安装包生成已可用，但应用启动和设置功能在不同环境下还需验证。
- **功能缺失**：实践模式（Practice Modes）、角色记忆（Character Memory）等功能尚未完成（仅为原型或待开发）。UI 个性化（亮色主题）和统计/成就系统也是后续规划。
- **性能/扩展性**：当前未进行大规模性能评估。随着会话长度和记忆库增长，SQLite 查询速度和前端渲染可能成为瓶颈。客户端直播语音链路的延迟优化（如使用 VAD 打断）也待增强。
- **安全/隐私**：存储在本地的敏感数据有一定保护（Keyring 保存 API 密钥），但应评估数据库导出（`speakmate.db`）的安全性。当前 CLI 明确不打印密钥，写操作需 `--yes`。对隐私而言，应明确哪些数据可上传到网络（LLM 查询、语音文本）和如何清理本地数据。
- **兼容性限制**：Edge TTS 仅限 Windows 平台，其他平台依赖浏览器 TTS 回退；同理 Whisper STT 若无法调用外部服务，需浏览器 STT 支持。多语言界面仅支持中英。用户可能期待多系统（Mac、Linux）和多语言（其它语言）兼容性。

# 二、问题与改进空间分析

结合代码结构、Issue 历史和文档规划，识别出以下技术债、瓶颈与风险：

- **技术债务**：品牌/命名遗留问题严重。文档和代码中混用 “SpeakMate” 与 “VoxVerse” 名称（如 CLI 名称、数据库文件名、日志文件名等）。这会给后续迭代和用户迁移带来混乱，需尽快统一。部分配置与路径兼容前旧版本逻辑（`com.ai-speaking.app`）也需清理。源代码中缺少 TypeScript 导入、类型声明等造成构建中断，属于欠缺的稳固性保障。
- **性能瓶颈**：前端使用 Vue3 合理，但当前大对话量情景下，`SessionHistory` 等组件可能因大量元素渲染而卡顿。后端连续的语音到文本和文本到语音处理较耗时，若没有并行管道（如边录音边处理上一次响应）可能延迟较高。SQLite 作为本地存储库，随着长时间使用、数据量增加，查询速度可能下降，需要评估索引和查询优化策略。
- **安全/隐私风险**：系统依赖第三方 LLM（OpenAI/OpenAI兼容、Ollama）和语音服务，网络交互潜在泄露聊天和学习内容。CLI 功能虽强，但用户操作需严格提示。尚需明确数据加密策略，比如导出的 `speakmate.db` 是否需要加密或用户授权。同时，Keyring 存储安全性需跨平台验证，必要时提供设置（如切换不同安全模块）的选项。
- **可用性/可访问性**：当前界面支持中文/英文。需要检查界面对屏幕阅读器等辅助工具的友好性（如为 TTS/录音按钮提供替代文本）。语音链路若中断（如网络断开时的降级）需要更友好的交互提示。目前仅有边界状态提示，后续可完善无障碍支持。
- **兼容性限制**：主打桌面应用，依赖 Edge TTS 意味着 Windows 功能更全面，而 Mac/Linux 用户则只能使用浏览器 TTS。建议评估替代方案（如 MacOS 语音API或其他 TTS 引擎）。同理，Whisper STT 在离线模式下不可用，需提供替代（如使用浏览器 STT 备选）。版本升级时要注意旧数据兼容，如数据库模式改变需迁移脚本。

# 三、v0.3 功能与改进建议

基于以上分析和项目长期规划（参考《升级计划V1.md》），提出下列 v0.3 目标功能和改进。每项列出优先级 (高/中/低)、估算工作量（人日）、主要风险及依赖关系：

| 改进项                     | 类型             | 优先级 | 估算 (人日) | 风险/依赖                               |
|---------------------------|----------------|------|-----------|------------------------------------------|
| **1. 完成 V0.1/V0.2 基线收口**       | 稳定性         | 高    | 5         | 无重大风险；依赖现有代码                     |
|- 修复构建错误（i18n 导入、类型等）    | 构建可靠性       | 高    | 2         | 文档不足，需小心代码回归                    |
|- 核实 Edge TTS/Browser TTS 回退        | 功能验证        | 高    | 3         | 需真实环境测试，可能受浏览器能力限制         |
|- Ollama/OpenAI 端点最终验收          | 功能验证        | 中    | 2         | 依赖有效 API 密钥和环境                    |
|- 更新文档中“SpeakMate”术语         | 文档           | 中    | 2         | 涉及多处文档、CLI 文案一致性               |
| **2. 统一命名 & 清理技术债**         | 架构维护       | 高    | 3         | 风险低；要保证兼容老用户迁移                |
|- 重命名 CLI、日志及数据库文件        | 架构/兼容性     | 高    | 3         | 更名后需提供迁移指南；测试旧数据迁移      |
|- 移除遗留路径检查、兼容逻辑        | 架构           | 中    | 2         | 如用户依赖需提供警告; 依赖改名成功           |
| **3. 性能优化**                    | 性能提升       | 中    | 5         | 需度量，可能并发问题; 依赖测试覆盖缺口         |
|- 前端分页/虚拟列表 (SessionHistory) | 前端优化       | 中    | 3         | 影响用户体验，需要兼容所有环境              |
|- 异步流式管道优化 (STT→LLM→TTS)    | 后端优化       | 中    | 5         | 需并行处理，风险时序错误; 依赖 tokio         |
|- 增加关键查询索引 (SQLite)        | 数据库优化     | 低    | 2         | 无风险；需评估查询模式                      |
| **4. API/接口改进**                | 接口设计       | 高    | 5         | 需兼容旧版 API; 文档和示例需更新            |
|- REST/WebSocket 服务化（CLI & Agent） | 接口扩展       | 中    | 5         | 架构大改；开发复杂，先期原型验证            |
|- CLI 输出字段稳定性审查           | CLI兼容性     | 高    | 3         | 易遗漏字段变更，需全量测试                  |
|- 拓展语言/平台支持配置项         | 可配置增强     | 中    | 3         | 需完整测试多系统和多语种                  |
| **5. 功能增强**                    | 新功能         | 高    | 10        | 功能范围广；依赖设计和用户反馈              |
|- **练习模式**：完善场景模板 (自由对话、面试等) | 产品/体验     | 高    | 5         | 依赖后端模式数据和场景设计                  |
|- **结构化反馈**：增加纠错、评分等反馈输出 | AI 功能       | 中    | 4         | 需额外 API 调用和模型支持                  |
|- **角色记忆**：长期记忆摘要/清除功能 | AI/体验       | 中    | 5         | 需设计 UI，使用 LLnM 提炼摘要有成本         |
| **6. 安全与隐私**                  | 安全           | 高    | 4         | 需细节评审；可能影响体验                   |
|- 数据库/音频加密选项 (可选)         | 隐私           | 中    | 3         | 性能开销；需用户同意                         |
|- 模式 & 审计日志加固               | 安全审计       | 中    | 2         | 基于现有 audit 已有实现                     |
| **7. 测试与文档**                  | 质量保证       | 高    | 6         | 人手瓶颈；依赖 CI/CD 平台                   |
|- 单元测试覆盖提升 (前端/后端)      | 测试           | 中    | 4         | 开销较大，优先核心逻辑                      |
|- 发布说明 & 示例日志模板         | 文档/流程     | 高    | 2         | 无风险；需整理变更点                      |
|- 用户指南更新 (中文/英文)         | 文档           | 高    | 3         | 时间可控；需 QA 审校                      |

> **说明：** 上表只列出主要功能与优化项。每项工作量估算为多人团队并行下所需人日，实际依赖人员经验和并行度。优先级以对项目健康和用户价值影响评估，高优先级项应尽快完成。

# 四、v0.3 里程碑与时间表

建议采用敏捷开发周期，将 v0.3 发布分成若干 Sprint：

| 周期/Sprint | 时间范围           | 主要交付物                                   | 验收标准                                 |
|-------------|----------------|------------------------------------------|--------------------------------------|
| **Sprint 1：基线冻结 (1周)** | 6月5日–6月11日  | - 修复编译和测试错误<br>- 完成 Lv.4 功能验收<br>- 更新文档术语（VoxVerse） | `pnpm build`、`cargo test` 全通过；Lv.4 验收 Checklist 完成 |
| **Sprint 2：稳定发布准备 (2周)** | 6月12日–6月25日 | - 角色系统与会话功能回归测试<br>- CLI 重命名与兼容性验证<br>- 准备 v0.2 发布说明草案 | 全流程手工测试通过；CLI 输出无大变更；发布说明覆盖核心改动 |
| **Sprint 3：核心新功能 (2周)** | 6月26日–7月9日  | - 实现练习模式模板（Free Talk, Interview 等）<br>- 结构化反馈优化<br>- 增加测试用例覆盖 | 新功能通过单元和手动验收；练习模式正确启动，反馈有效生成 |
| **Sprint 4：优化与完善 (2周)** | 7月10日–7月23日 | - 前端性能优化（虚拟列表）<br>- 数据库索引及查询优化<br>- 添加安全/隐私选项 | 性能测评无明显回退；新选项文档化；无安全警告 |
| **Sprint 5：发布与后续**     | 7月24日–7月30日 | - 完整回归测试<br>- 发布说明撰写、内部评审<br>- 回滚方案制定 | 所有关键功能/用例通过测试；回滚机制验证可行 |

```mermaid
gantt
dateFormat  YYYY-MM-DD
title v0.3 开发里程碑
section 基线冻结
编译&测试修复 :done, 2026-06-05, 3d
Lv.4 功能验收 :done, after compile , 2d
文档统一与术语检查 :done, after compile , 2d
section 稳定发布准备
角色/会话 回归测试       :done, 2026-06-12, 5d
CLI 重命名&兼容验证    :done, after , 5d
发布说明草案            :done, after , 4d
section 核心新功能
练习模式实现           :done, 2026-06-26, 7d
结构化反馈改进         :done, after , 7d
测试用例扩充           :done, after , 3d
section 优化与完善
前端性能优化           :done, 2026-07-10, 7d
数据库索引优化         :done, after , 3d
安全/隐私选项添加       :done, after , 4d
section 发布与回滚
回归测试与验收         :done, 2026-07-24, 5d
发布说明与沟通文档       :done, after , 3d
回滚方案准备           :done, after , 2d
```

**验收标准与回滚：** 每个 Sprint 末尾进行自动化和人工测试回归，确保 `pnpm build`、`cargo check/test` 全绿灯；关键功能（对话流、角色导入导出、练习模式等）可正常使用。v0.3 发布说明应列出新增功能、改动和已知问题。回滚策略：新版本发布前保存数据库和配置快照，若出现严重 BUG 可降级至 v0.2 代码；代码回退至上一个稳定 tag，通知用户根据快照恢复数据。 

# 五、资源需求与风险评估

- **人员角色：** 需要前端工程师（精通 Vue3+TypeScript）、后端工程师（精通 Rust、Tauri）、QA 测试人员、文档/产品人员。AI 功能（LLM、语音）相关可由全栈工程师跨岗完成，视团队规模调整。项目经理/Scrum 主持人负责里程碑跟进。
- **技能要求：** 熟悉 Tauri 框架、Rust 异步编程、SQLite、Vue 组件化开发、CI/CD 工具链。了解语音API使用（Whisper Tauri 插件、Edge TTS Rust 库）、LLM 调用（OpenAI API / Ollama）。
- **CI/CD 建议：** 建立 GitHub Actions 流程，覆盖 Windows/macOS/Linux 的构建与测试：包括 `pnpm install && pnpm build`、Rust `cargo check --all-targets`、`cargo test`。增加代码质量检查（clippy、格式化、依赖漏洞扫描如 `cargo audit` 和 `npm audit`）。发布流程可集成自动打包和签名。建议引入测试覆盖报告和性能基准测试，以量化改进效果。
- **自动化测试：** 开发核心单元测试（如对话流逻辑、数据库 CRUD、CLI 输出稳定性），集成端到端测试（模拟用户会话、语音流、错误处理）。可以借助 Puppeteer 之类工具进行前端 UI 验证。每次关键提交后执行全量 Smoke Test。
- **外部库与许可风险：** 所有依赖主要是开源组件（MIT/Apache 等常见许可），需确认如 `msedge-tts` 是否有商业限制。目前未见闭源组件。外部依赖（OpenAI、Ollama 等）服务条款需注意用户数据隐私（可能要求不能收集敏感信息）。未来若接入第三方 Avatar（V4 计划），需评估隐私和费用机制。

# 六、迁移与兼容性说明

- **数据库迁移：** 如果实施了数据库结构变化（如角色表或会话模式），应提供迁移脚本（Rust/SQL）或在首次启动时自动升级。例如，从 v0.2 升级后自动备份并修改表结构，确保老数据保留。若重命名数据库文件（`speakmate.db` → `voxverse.db`），应在启动时检查旧文件并提示迁移。
- **配置兼容：** 旧版配置（如 Tauri Store）自动合并新字段；不存在配置时使用默认。若修改 CLI 命令（更名），应保留软链接或兼容前缀，避免瞬间破坏现有脚本。新的命令帮助文档应注明旧命令替代关系。
- **示例变更日志模板：** 发布说明（CHANGELOG）应包含“Added/Changed/Fixed”分类。例如：

  ```md
  ## v0.3（日期）
  - **新增**：练习模式“面试练习”；可在角色对话中选择不同模式模板。
  - **新增**：对话完成后提供结构化反馈（纠错、评分、单词提示）功能。
  - **优化**：前端会话历史使用虚拟列表渲染，改善大会话时的性能。
  - **修复**：解决了 App.vue 缺少 i18n 导入导致的构建错误；修正了多语言界面类型不匹配的问题。
  - **更新**：所有文档和命令将“SpeakMate”替换为“VoxVerse”；CLI 入口程序名从 `speakmate` 改为 `voxverse` 并兼容旧命令。
  - **安全**：增加数据库加密选项（需用户开启）；写操作需 `--allow-network` 标志。
  - **已知问题**：Linux 上的 Edge TTS 功能仍不可用，将回退至浏览器 TTS。
  ```

- **用户沟通要点：** 发布时要明确新版本兼容性和改动，指导用户如何安装升级包、验证数据迁移；强调隐私改进（本地存储加密等）；推荐重读更新文档。若有重大改动（如 CLI 名称变更），提前在社区公告，避免误操作。提供回滚说明（使用版本管理功能或下载旧版安装包）以降低升级风险。

  
**总结：** 以上为 VoxVerse v0.3 的升级计划与路线图，覆盖现状评估、问题分析、功能改进、时间表及相关资源和流程建议。所有内容均基于项目现有代码和文档进行归纳，如无明确描述的部分已标记为“未指定”或根据实现推断。后续执行过程中需密切跟踪验证每项改进的效果，并及时更新文档和路线图。