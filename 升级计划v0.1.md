# VoxVerse 升级计划

> 更新时间: 2026-05-29  
> 范围: 从当前 SpeakMate / Tauri 桌面应用基线升级为 VoxVerse  
> 输入依据: `VoxVerse 技术路径报告.md`、`task.md`、`implementation_plan.md`、`upgrade-plan.md`、当前代码结构  
> 执行规则: 本文件只定义升级路线，不代表已经执行升级

## 1. 总体结论

VoxVerse 的升级方向不是继续给当前 SpeakMate 叠加零散功能，而是把产品从“AI 口语训练桌面应用”推进为“voice-first 角色宇宙”。核心路线是:

1. 先冻结并验收当前 V0.1 / V0.2 工程基线。
2. V1 做稳定的实时语音角色体验和结构化记忆。
3. V2 加入异步图像 / 视频生成，让对话产出可保存、可分享。
4. V3 统一角色资产标准，沉淀 VRM / glTF、动作、服装和表情资产。
5. V4 再把实时数字人作为 premium 展示层接入。

最重要的工程原则是: 实时链路只处理语音、字幕、角色状态和工具事件；图像、视频、3D 和数字人都必须解耦为异步或高级输出层，不能阻塞基础对话体验。

## 2. 当前基线

当前仓库已经具备以下可复用基础:

- Tauri 2 + Vue 3 桌面应用骨架。
- OpenAI-compatible 流式聊天链路。
- 角色系统、角色导入导出、SQLite 持久化。
- 会话历史、消息保存、场景、练习模式、反馈、生词本、统计原型。
- Provider Profiles、Keyring 密钥隔离、Ollama / Custom provider 配置。
- Whisper STT、Edge TTS、Browser STT / TTS fallback。
- Native CLI 与 JSONL / MCP-style agent 控制面。
- V0.1 验证脚本、Lv.4 验收清单和构建日志体系。

当前仍需先收口的事项:

- 真实 OpenAI-compatible endpoint、Whisper STT、Ollama、Browser STT / Edge TTS fallback 的人工验收。
- 安装包启动、Settings、聊天、语音入口的人工确认。
- `lv4-validation-checklist.md` 中真实端点和桌面交互结论补齐。

## 3. 升级原则

1. 先冻结，再升级: 未完成 V0.1 / Lv.4 人工验收前，不进入 WebRTC / LiveKit / 生成队列等大改造。
2. 语音优先: V1 目标是“真的想继续说下去”的实时角色体验，不先做全量数字人。
3. 记忆可解释: 长期记忆必须可查看、可删除、可限制注入，不把全部聊天记录直接塞进 prompt。
4. 异步生成: 图像、视频和 3D 资产全部走任务队列，返回 `job_id`，不阻塞实时会话。
5. 供应商可替换: STT、TTS、LLM、图像、视频、avatar 都要通过 provider 抽象接入。
6. 本地优先延续: 当前 SQLite、Keyring、CLI 审计边界继续保留，云端化要分阶段迁移。
7. 高价能力配额化: OpenAI Realtime、视频生成、实时数字人只作为 premium 或按量能力开放。

## 4. 阶段路线

| 阶段 | 目标 | 主要交付 | 不做内容 |
| --- | --- | --- | --- |
| P0 基线冻结 | 把当前 SpeakMate 收束为可靠基线 | V0.1 / Lv.4 人工验收、安装包验证、已知限制记录 | 不新增 Realtime、生成队列、数字人 |
| V1 语音角色 MVP | 建立实时语音角色体验 | WebRTC / LiveKit 研究与接入、字幕、打断、结构化记忆、工具路由 | 不上线全量实时数字人 |
| V2 创作扩展 | 把对话转为可保存作品 | 图像生成队列、视频生成队列、作品页、审核、分享链路 | 不做复杂 3D 资产生产 |
| V3 角色资产化 | 让角色可复用、可穿戴、可成长 | VRM / glTF 标准、角色资产库、动作 / 表情 / 服装槽位、3D viewer | 不把 photoreal 数字人作为默认体验 |
| V4 实时数字人 | 高沉浸 premium 输出层 | HeyGen / Tavus / Runway Characters 等 avatar provider 接入、权限与计费 | 不替换 V1 主会话协议 |

## 5. P0: 当前基线冻结

### 目标

在进入 VoxVerse 架构升级前，先让当前 SpeakMate 成为稳定、可安装、可回归的工程基线。

### 必做事项

- 运行并保留 `pnpm validate:v0.1:full` 报告。
- 完成真实 OpenAI-compatible 聊天、Keyring、Whisper STT 验收。
- 在可用环境中完成 Ollama chat compatibility 验收。
- 验证 Edge TTS、Browser TTS fallback、Browser STT fallback。
- 安装 NSIS 或 MSI 后验证启动、Settings、聊天和语音入口。
- 将结论写入 `lv4-validation-checklist.md`。

### 完成标准

- `pnpm validate:v0.1:full` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 通过。
- README 的 Known Limitations 与真实验收结果一致。

## 6. V1: 语音角色 MVP

### 目标

把当前“点击发送 + TTS 播报”的聊天体验升级为低延迟、可打断、有字幕、有关系记忆的实时语音角色体验。

### 主要功能

- 实时接入层: 评估并接入 WebRTC / LiveKit，用于浏览器或未来 Web 客户端音频采集。
- 会话编排器: 解耦 STT、LLM、TTS、记忆检索和工具调用。
- 实时字幕: 支持 `transcript.partial` 与 `transcript.final`，处理 partial 抖动和 final 覆盖。
- 打断机制: 引入 VAD / turn detector，支持用户抢话、停止播报和中断当前 assistant 输出。
- 角色关系状态: 记录亲密度、信任、剧情阶段、用户偏好、禁区和承诺。
- 四层记忆: 原始 turn、事件层、长期事实层、检索层。
- 工具路由器: 将“生成图片”“生成回顾”“保存记忆”等能力统一为工具事件。

### 建议事件协议

| 事件 | 用途 |
| --- | --- |
| `session.started` | 实时会话创建成功 |
| `audio.input` | 客户端音频输入开始或持续 |
| `transcript.partial` | STT 临时字幕 |
| `transcript.final` | STT 最终字幕 |
| `turn.interrupted` | 用户打断或系统中断 |
| `assistant.delta` | assistant 文本 / 音频增量 |
| `assistant.done` | assistant 本轮结束 |
| `tool.requested` | LLM 请求工具调用 |
| `runtime.error` | 实时链路错误 |

### 数据模型方向

- `relationship_state`: 角色与用户的长期关系状态。
- `memory_facts`: 可解释长期事实，支持来源、置信度、可见性和删除。
- `memory_embeddings`: 记忆检索向量，可先保留抽象层，后续接 pgvector / Qdrant。
- `tool_invocations`: 工具调用记录，包含状态、输入、输出、错误和审计信息。

### 验收标准

- 用户能完成连续语音对话，并看到 partial / final 字幕。
- 用户能中断 assistant 播报，系统不会重复保存错乱消息。
- 角色切换后记忆不混用。
- 用户能查看、删除或禁用重要记忆。
- 默认语音链路成本、p50 / p95 首包延迟、STT partial 延迟有记录。

## 7. V2: 创作扩展

### 目标

让用户在对话中自然触发图像、短视频、剧情 recap 和角色设定卡生成，把“聊完”变成“可产出、可保存、可分享”。

### 主要功能

- 图像生成队列: 角色图、剧情图、封面图、设定图。
- 视频生成队列: 3 到 8 秒剧情片段、章节回顾、PV 模板。
- 作品页: 展示生成结果、来源会话、角色、版本和状态。
- 分享链路: 生成分享链接，公开发布前走更严格审核。
- 审核系统: 文本、图像、视频和角色模板分层审核。
- 失败恢复: 支持任务失败重试、降级 provider 和用户可读错误。

### 接口规则

- 会话内工具调用只返回 `job_id`。
- 生成结果通过 polling、webhook 或实时 socket 事件回推。
- 所有产物统一进入 `artifacts` 模型。
- 私密作品和公开作品必须分开治理。

### 数据模型方向

- `generation_jobs`: 生成任务状态、provider、成本、重试次数。
- `artifacts`: 图像、视频、音频、3D 文件等统一资源。
- `artifact_versions`: 同一作品的不同生成版本。
- `scene_cards`: 从对话提炼出的剧情片段和镜头描述。
- `share_links`: 分享状态、过期时间、可见性。
- `moderation_events`: 审核结果、命中规则、人工复核记录。

### 验收标准

- 图像 / 视频任务不会阻塞实时语音会话。
- 任务状态可见，失败原因可读。
- 公开分享前有审核记录。
- 每次生成的平均成本和成功率可统计。

## 8. V3: 角色资产化

### 目标

将角色从聊天 prompt 升级为可复用资产实体，逐步沉淀 2D 设定、语音、视频、VRM / glTF、服装、动作和表情。

### 主要功能

- `canonical_character_id`: 所有角色相关资产共用的稳定主键。
- 角色资产库: 2D 图、语音风格、视频片段、VRM / glTF 文件统一管理。
- VRM / glTF 导出: 以 VRM 1.0 + glTF 作为长期资产标准。
- 自动骨骼绑定: AccuRIG 为主，Mixamo 为备选。
- 3D viewer: 桌面端或 Web 端预览角色、动作、表情和服装。
- 轻量 lip-sync: 先做非实时预览，后续再接实时 avatar。

### 数据模型方向

- `character_assets`: 角色资产主表。
- `rig_profiles`: 骨骼绑定和 retarget 配置。
- `motions`: 动作库。
- `wardrobe_items`: 服装和外观槽位。
- `expression_sets`: 表情集和口型数据。

### 验收标准

- 同一角色的图像、视频、3D、语音能通过主键关联。
- VRM / glTF 文件可下载或预览。
- viewer 在目标设备上保持可接受 FPS。
- 资产生成失败可回滚，不污染角色主数据。

## 9. V4: 实时数字人

### 目标

把实时数字人作为高沉浸、高成本、高客单价的 premium 输出层，而不是基础链路。

### 主要功能

- Avatar provider 抽象: HeyGen、Tavus、Runway Characters 等通过统一接口接入。
- Avatar session: 与 V1 会话协议解耦，只消费角色状态、文本、语音和工具事件。
- 摄像头 / 屏幕感知: 默认关闭，用户授权后才启用。
- 高级合规: 自定义 voice / avatar 需要 consent 证据链。
- 配额与计费: 实时数字人分钟数单独计费或进入高级订阅。

### 数据模型方向

- `avatar_sessions`: avatar 会话状态和分钟成本。
- `avatar_profiles`: avatar provider、角色映射和质量档位。
- `perception_events`: 摄像头、屏幕或多模态感知事件。
- `compliance_exports`: 授权、审计和导出记录。

### 验收标准

- avatar 故障不会影响纯语音会话。
- 用户可在 voice / avatar 输出层之间切换。
- speech-to-video 延迟、并发占用、分钟成本有监控。
- premium 配额扣减准确，超额时优雅降级。

## 10. 公共接口与抽象

建议新增或重构以下后端抽象:

- `RealtimeSessionProvider`: 实时房间、音频流、turn-taking、session event。
- `SttProvider`: Deepgram、AssemblyAI、OpenAI、ElevenLabs 等转写供应商。
- `TtsProvider`: Edge TTS、ElevenLabs、Cartesia、OpenAI audio 输出。
- `LlmOrchestrator`: prompt、记忆注入、工具调用、错误恢复。
- `MemoryService`: turn 摘要、事件抽取、长期事实、检索。
- `ToolRouter`: 工具注册、幂等、权限、超时、审计。
- `GenerationProvider`: 图像 / 视频生成 provider 统一接口。
- `AssetProvider`: VRM / glTF / 资产转换 provider 统一接口。
- `ModerationService`: 私密会话和公开发布的分层审核。

## 11. 测试与验收策略

### 自动化测试

- Rust provider 配置、Keyring、安全边界、session、message、feedback、CLI gate 单元测试。
- 前端 composable 状态测试，覆盖语音 runtime、任务状态和错误恢复。
- 生成队列使用 mock provider 做成功、失败、超时和重试测试。

### 手动验收

- 真实端点: OpenAI-compatible、Whisper、Ollama、TTS / STT fallback。
- 实时语音: 弱网、长会话、打断、重复发送、设备切换。
- 生成任务: 图片、视频、审核、分享、删除。
- 资产流程: VRM / glTF 预览、下载、失败回滚。
- 数字人: 权限授权、分钟计费、provider 故障降级。

### 监控指标

- p50 / p95 端到端首包延迟。
- STT partial 延迟和 final 覆盖率。
- TTS 首字节延迟。
- 打断成功率和误打断率。
- 每分钟语音成本。
- 生成任务排队时长、成功率和平均成本。
- 内容审核命中率。
- avatar 并发、分钟成本和 premium 转化率。

## 12. 成本与商业门禁

默认目标规模按技术报告中的 1k DAU、峰值并发 50 估算。建议采用以下成本门禁:

- 默认语音链路走解耦 STT / LLM / TTS。
- OpenAI Realtime 仅用于 premium 高沉浸模式。
- 图像生成提供免费低额度，更多进入订阅或点数。
- 视频生成只做 premium / 异步配额，不开放无限使用。
- 实时数字人不进入免费层，不作为 V1 默认能力。

商业层建议:

- 免费层: 文本、少量语音分钟、有限角色位、低配图像额度。
- 基础订阅: 更长实时语音分钟、更多记忆槽位、更多角色位。
- 创作订阅: 图像 / 视频额度、剧情回顾、导出、素材包、工作流模板。
- Premium 沉浸层: 实时数字人分钟、高清 avatar、自定义 voice / 角色资产。

## 13. 风险与缓解

| 风险 | 表现 | 缓解 |
| --- | --- | --- |
| 延迟失控 | 用户说完等待过久、字幕乱跳、误打断 | WebRTC、partial / final 字幕、turn detector、图像视频异步化 |
| 记忆膨胀 | 越聊越贵、越聊越乱、错记用户偏好 | 四层记忆、定期压缩、可查看和删除记忆 |
| 成本失控 | 视频、Realtime、数字人消耗过高 | 分层路由、配额、预算墙、premium 化 |
| 单供应商依赖 | 涨价、限流、停运 | provider 抽象、多供应商 fallback、资产标准化 |
| 合规风险 | 真人仿冒、未成年人、高风险公开内容 | 私密 / 公开分层审核、自定义素材 consent 证据链 |
| 资产一致性差 | 同一角色多次生成外观漂移 | `canonical_character_id`、reference bundle、style pack、资产回注 |

## 14. 推荐实施顺序

1. P0: 完成 V0.1 / Lv.4 人工验收并冻结当前基线。
2. V1-A: 设计实时事件协议和后端 provider 抽象。
3. V1-B: 扩展角色关系状态和四层记忆模型。
4. V1-C: 接入 WebRTC / LiveKit 原型，验证字幕、打断和延迟。
5. V1-D: 将现有 ChatPanel 改造为角色舞台式语音体验。
6. V2-A: 新增异步生成队列和 `artifacts` 资源模型。
7. V2-B: 接入首个图像 provider，再接入视频 provider。
8. V2-C: 做作品页、审核和分享链路。
9. V3-A: 建立 `canonical_character_id` 与角色资产库。
10. V3-B: 接入 VRM / glTF 预览和导出。
11. V4-A: 小流量压测实时数字人 provider。
12. V4-B: 将数字人作为 premium 输出层上线。

## 15. 明确不在第一阶段执行的内容

- 不把 HeyGen / Tavus / Runway Characters 做成 V1 默认输出层。
- 不在 V1 全量开放视频生成。
- 不在没有审核和配额前做公开作品社区。
- 不直接放弃当前 Tauri / SQLite / Keyring / CLI 基线。
- 不把所有历史聊天直接放入向量库。
- 不把 `upgrade-plan.md` 直接覆盖掉；它继续作为旧 SpeakMate 升级历史文档。

