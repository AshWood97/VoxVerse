# 1. 项目当前状态判断

## 1.1 项目用途  
VoxVerse 是一个“以语音为中心”的角色扮演口语学习应用，用于**沉浸式对话练习与学习反馈**。用户可以与 AI 驱动的角色进行流式对话，系统支持角色定义、会话历史、生词本、纠错反馈等功能，强调本地化隐私保存（SQLite、本地 Keychain 等）。

## 1.2 技术栈  
项目基于 **Tauri 2 + Vue 3** 桌面应用框架。前端使用 Vue 3、TypeScript、Volar 插件、多语言 (i18n)；后端使用 Rust（`rusqlite`、`tokio`、`keyring`、`msedge-tts` 等库）通过 Tauri IPC 提供命令接口。使用 Node.js (pnpm) 管理前端依赖和脚本，数据库选 SQLite，本地 Keyring 存储 API Key。AI 服务通过兼容 OpenAI 的接口、Whisper STT、Edge TTS 以及 Ollama 本地 LLM 等提供。

## 1.3 关键目录与文件  
- **前端代码**：`src/` 包含 Vue 组件和业务逻辑：`App.vue` 是应用主界面协调层，组件目录 (`src/components/base`, `domain`) 包含聊天面板、反馈面板、会话历史等业务组件；组合式钩子 (`src/composables/useChat.ts`、`useCharacter.ts` 等) 管理状态；`src/services/tauri/` 封装了对 Rust 命令的 IPC 调用；多语言文件在 `src/i18n.ts` 和 `src/locales/`。  
- **后端代码**：`src-tauri/src/` 包含 Rust 后端：`lib.rs` 是 Tauri 应用入口，负责注册所有命令；`src-tauri/src/commands/` 存放前后端桥接命令实现；`src-tauri/src/repositories/` 处理 SQLite 数据库读写持久化；`src-tauri/src/services/` 实现聊天、反馈、STT、TTS 等服务；`src-tauri/src/state/` 管理运行时状态和 Keyring 配置。  
- **配置与脚本**：`src-tauri/tauri.conf.json` 配置打包（已开启多平台打包、包含 `.icns` 图标）；根目录下有 `README.md`、升级规划文档 (`task.md`、`implementation_plan.md` 等)、验证脚本（如 `scripts/validate-lv4.ps1`）和 Windows 启动脚本 `LaunchVoxVerse.bat`。关键文件包括：`src/main.ts`（前端启动）、`src-tauri/Cargo.toml` (Rust 依赖)、Tauri 命令代码等。

## 1.4 核心运行链路  
开发者通过 `pnpm tauri dev` 在本地启动应用（前端热重载 + Rust 后端）。用户界面发起语音或文本输入，前端通过 Tauri IPC 调用后端 STT (Whisper 或 Web API) 将语音转为文本，然后后端调用 LLM 生成回复文本，再调用 TTS (Edge 或浏览器语音) 输出语音。所有对话消息按顺序存 SQLite 数据库 (`chat_sessions`, `messages` 等表)。前端提供会话历史视图、学习反馈面板和角色管理功能。原生 CLI (`voxverse-cli`) 通过 JSON 输出提供对本地数据库的只读查询和受控写入（会话、配置切换等）。

## 1.5 当前最大风险  
**跨平台兼容性不足**：当前项目的本地启动器与文档主要围绕 Windows 平台，macOS Apple Silicon 仍缺少明确的构建、运行和验收记录。应用主数据库层已经使用 Tauri 的 `app_local_data_dir()`，但原生 CLI 的数据库发现逻辑仍偏向 Windows `%LOCALAPPDATA%`，在 macOS 上可能找不到 `~/Library/Application Support/com.voxverse.desktop/voxverse.db`。从 v0.5 起，本轮目标明确为 **Windows Intel/AMD 64-bit** 与 **macOS Apple Silicon 本地 `.app`**；Windows 32 位、DMG、签名、公证、自动更新不纳入本轮。TTS 策略保持“Edge TTS 优先尝试 + 浏览器 `speechSynthesis` 回退”，风险点是需要在 macOS 上真实验收该回退链路，而不是假设 `msedge-tts` 只能在 Windows 上运行。

# 2. 发现的问题列表

| 优先级 | 问题 | 证据位置 | 影响 | 推测原因 | 置信度 | 建议处理方式 |
|---|---|---|---|---|---|---|
| P0 | **macOS Apple Silicon 缺少构建/运行验收**：当前文档和代码主要服务 Windows，本轮需要明确 macOS `.app` 构建流程。 | README；Tauri 打包配置；本计划 | Mac 开发者缺少可重复运行路径 | 未补充 Apple Silicon 本地构建说明和 CI 验证 | 高 | 补充 macOS 依赖、`pnpm tauri dev`、`pnpm tauri build`、`.app` 路径说明；在 CI 加入 macOS latest 的 build/test。 |
| P1 | **Windows 平台目标表述含混**：原计划使用 “Windows x86” 容易被理解为 32 位，但本轮只支持 Intel/AMD 64-bit。 | README；本计划 | 发布和测试矩阵被误扩展到 i686，增加无收益复杂度 | 未明确平台命名 | 高 | 文档统一写为 Windows Intel/AMD 64-bit；明确 Windows 32 位不纳入 v0.5。 |
| P1 | **CLI 仅搜索 Windows 数据库路径**：`resolve_db` 函数只检查 `LOCALAPPDATA` 下的路径，未考虑 macOS/Linux 的 `$HOME/Library/Application Support` 等路径。 | 源码 `voxverse.rs` 中 `resolve_db` 仅使用 `LOCALAPPDATA` | macOS 下 CLI 命令找不到数据库，状态报告失败 | 缺少 macOS 平台对应路径逻辑 | 高 | 修改 CLI 代码：在 `resolve_db` 中加入对 macOS/Linux 用户目录的检查（如 `$HOME/Library/Application Support/com.voxverse.desktop/voxverse.db`），确保 CLI 在 Mac 下能定位数据库。 |
| P1 | **TTS 需要 macOS 真实验收**：现有前端已有浏览器 `speechSynthesis` 回退，但尚未在 Apple Silicon 上记录验收。 | `src/composables/useSpeechSynthesis.ts`；`src-tauri/src/services/tts.rs` | macOS 上若 Edge TTS 失败，需要确认浏览器回退可用且错误提示清晰 | 缺少目标平台人工验收记录 | 中 | 保留现有 Edge TTS 尝试与浏览器回退，不增加平台硬禁用；在 macOS 验收步骤中记录 Edge TTS 成功或浏览器回退成功。 |
| P2 | **缺少 macOS 构建与部署文档**：README 和Quick Start 只列出了 Windows 指南。 | README Quick Start 部分 | 团队/用户无 macOS 构建参考，易出错 | 文档中未包括 Mac 平台步骤 | 中 | 更新文档：在 Quick Start 中添加 macOS (M 系) 环境下的安装依赖与构建步骤；提供 macOS 启动方式说明（如 `.app` 位置）。 |
| P2 | **测试覆盖与 CI 缺失 Mac 平台验证**：现有自动化验证未包括 macOS，仅通过本地脚本；缺少前端单元/端到端测试。 | 文档建议 | Mac 构建回归难以及早发现；总体测试不足 | 侧重 Windows 环境，未全面覆盖 | 低 | 在 CI 中加入 macOS 构建与测试（如 GitHub Actions macOS runner）；添加前端单元测试与 e2e 测试，提升质量。 |

# 3. Debug 与验证计划

## 3.1 目标平台  
- **Windows Intel/AMD 64-bit**：保持现有 Windows 主目标，继续验证 `LaunchVoxVerse.exe` / `LaunchVoxVerse.bat` 和常规 Rust/Vue 构建。  
- **macOS Apple Silicon**：在 M 系 Mac 上运行 `pnpm tauri dev` 和 `pnpm tauri build`，交付本地可启动的 `VoxVerse.app`。  
- **明确不做**：Windows 32 位、Intel macOS 强制验收、DMG、签名、公证、自动更新。  

## 3.2 验证顺序  
1. **CLI 数据库发现**：运行 `pnpm cli -- status --json` 或 `pnpm cli -- diagnostics run --json`，确认 `db.candidates` 包含 `macOS com.voxverse.desktop`，且在数据库存在时 `db.exists: true`。  
2. **构建验证**：Windows 与 macOS 均运行 `pnpm build` 和 `cargo test --manifest-path src-tauri/Cargo.toml`。  
3. **macOS `.app` 验证**：Apple Silicon 机器运行 `pnpm tauri build`，确认 `src-tauri/target/release/bundle/macos/VoxVerse.app` 可启动。  
4. **语音验收**：在 macOS 触发一次 TTS，记录 Edge TTS 成功或浏览器 `speechSynthesis` 回退成功；失败时保留前端错误提示与日志。  

# 4. 升级计划

## 4.1 短期修复  
- **CLI 跨平台路径**：给 `voxverse-cli` 增加 macOS Application Support 候选路径，同时保留 `--db`、`VOXVERSE_DB`、`SPEAKMATE_DB`、Windows `LOCALAPPDATA` 和 legacy SpeakMate 路径。  
- **文档完善**：README 与本计划统一说明 Windows 64-bit、macOS Apple Silicon、`.app` 路径、Keychain、SQLite 路径和 TTS 回退策略。  
- **CI 验证**：新增 GitHub Actions 风格的 Windows/macOS 最小矩阵，运行依赖安装、前端构建和 Rust 测试。  

## 4.2 中期整理  
- 把数据库路径候选生成逻辑继续收敛成共享函数，减少 CLI 与应用数据库迁移路径之间的重复。  
- 为前端语音回退链路补充更细的组件或端到端测试，尤其覆盖 WebView 不支持浏览器语音时的提示。  
- 记录 Apple Silicon 手工验收结果，包括 `.app` 启动、Keychain 写入、SQLite 路径、TTS 回退和 CLI 查询。  

# 5. 可执行任务队列

## Task-001：支持 macOS 上的 CLI 数据库路径检测  
**优先级**：P0  
**涉及文件**：`src-tauri/src/bin/voxverse.rs`  
**执行内容**：在 `resolve_db` 中加入 `~/Library/Application Support/com.voxverse.desktop/voxverse.db`，并补充 legacy macOS 候选路径。抽出可测试 helper，让单元测试可以注入环境变量和文件存在性。  
**验收标准**：单元测试覆盖 macOS current path、macOS legacy path、Windows `LOCALAPPDATA`、`--db` / `VOXVERSE_DB` 优先级；CLI JSON 字段结构保持不变。  

## Task-002：更新 README 和 v0.5 文档  
**优先级**：P1  
**涉及文件**：`README.md`、`升级计划v0.5.md`  
**执行内容**：补充 macOS Apple Silicon 依赖、开发启动、打包命令、`.app` 路径、Keychain/SQLite 路径和 TTS 回退说明；统一 Windows 目标为 Intel/AMD 64-bit。  
**验收标准**：文档不再要求 Windows 32 位、不再要求 DMG/签名/公证，不把 `msedge-tts` 写成 Windows-only。  

## Task-003：新增最小 CI 验证矩阵  
**优先级**：P1  
**涉及文件**：`.github/workflows/ci.yml`  
**执行内容**：新增 Windows latest 与 macOS latest 矩阵，安装 pnpm/Rust，执行 `pnpm install`、`pnpm build`、`cargo test --manifest-path src-tauri/Cargo.toml`。  
**验收标准**：CI 只做构建与测试，不发布、不签名、不公证。  

# 6. 最终验收清单

- `cargo test --manifest-path src-tauri/Cargo.toml` 通过。  
- `pnpm build` 通过。  
- `pnpm cli -- status --json` 在 Windows 本地可运行，并保留原有 DB 输出结构。  
- Apple Silicon 手工执行 `pnpm tauri dev`、`pnpm tauri build`，确认 `.app` 可启动。  
- Apple Silicon 手工验证 TTS：Edge TTS 成功或浏览器回退成功。  
