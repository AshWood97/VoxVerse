# SpeakMate V0.1 查缺补漏清单

> 更新时间: 2026-04-28
> 阶段目标: V0.1 Release Candidate
> 规则: 接下来只做本清单内的补全点；补完、验收通过后即冻结为 V0.1。未列入本清单的新能力全部延后。

## 结论

项目现在不是“功能不够”，而是需要把已经存在的功能补稳。V0.1 的目标是让基础聊天、角色、语音、反馈、会话、Provider Profiles、Native CLI/Agent 和本地学习闭环达到可测试、可安装、可解释的状态。

## 当前基线

- `pnpm validate:v0.1:full` 已通过，最新报告: `validation-reports/lv4-smoke-report-20260428-105925.md`。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 已通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 已通过，当前有 23 个 Rust 测试。
- 生产路径的 `Mutex::lock().unwrap()`、DB 初始化 `expect/unwrap` 已替换为可返回错误的路径。
- 自动化、代码补全、文档安全边界、known issues 已收口；剩余仅是真实端点、Ollama、桌面语音和安装后启动的人工验收。

## V0.1 必补清单

### A. 发布与命名收口

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-A1 | 已完成 | 文档和脚本曾混用 V1 / V0.1 命名 | 当前文档、脚本、日志入口统一为 V0.1；旧 V1 临时文件已移除 |
| V0.1-A2 | 已完成 | `validate:v1` 入口需要改为 V0.1 入口 | 提供 `pnpm validate:v0.1` 和 `pnpm validate:v0.1:full`，并在 README/task/implementation 中使用 |
| V0.1-A3 | 已完成 | V0.1 产物和报告路径需要明确 | 最新 full build 报告、MSI、NSIS 路径已写入构建日志和 README |

### B. Rust 稳定性与代码质量

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-B1 | 已完成 | 多处 `Mutex::lock().unwrap()` | Tauri command/repository 路径改为返回 `AppError`/`String`，不让用户操作触发 panic |
| V0.1-B2 | 已完成 | DB 初始化使用 `expect/unwrap` | 初始化错误转换为 Tauri setup error，并在错误中包含路径原因 |
| V0.1-B3 | 已完成 | `cargo clippy -D warnings` 不通过 | `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 通过 |
| V0.1-B4 | 已完成 | Rust 单元测试为 0 | 已补 provider/feedback/STT/TTS/session/CLI 安全边界等轻量测试，`cargo test` 当前 23 tests |
| V0.1-B5 | 已完成 | CLI 主文件过大但缺少内部测试 | 已补 CLI parse、allowlist、network gate、audit filter 测试 |

### C. 网络、语音与资源边界

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-C1 | 已完成 | LLM/STT/diagnostics/CLI 网络请求缺少统一 timeout | chat、health check、STT、CLI chat completion 已增加超时，并返回明确错误 |
| V0.1-C2 | 已完成 | STT 上传缺少音频大小和录音时长边界 | 前端限制 90 秒/25 MB，后端拒绝空音频、非音频 MIME 和超过 25 MB 的 payload |
| V0.1-C3 | 已完成 | TTS 文本长度无边界 | 后端拒绝超过 4000 字符的 TTS 文本；前端朗读时只取前 4000 字并提示 |
| V0.1-C4 | 待人工验收 | Browser STT / Edge TTS fallback 仍需真实桌面验证 | `lv4-validation-checklist.md` 填入真实测试结论；失败时记录 fallback 行为和诊断 |
| V0.1-C5 | 待人工验收 | Ollama 仍是环境警告 | 在安装 Ollama 的机器上完成一轮 chat 兼容验收；若不支持则在 V0.1 known issues 说明 |

### D. Provider Profiles 与密钥安全

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-D1 | 已完成 | Provider config 保存缺少 base_url/model 空值校验 | Settings 和 Rust command 都会校验 base_url/model，错误文案清晰 |
| V0.1-D2 | 已完成 | Profile 名称可重复 | 创建/重命名 profile 时阻止重名，UI 和 Rust command 双层保护 |
| V0.1-D3 | 已完成 | Profile 只有创建/切换/另存，缺少重命名/删除 | 已补 rename/delete；`default` profile 不可删除；删除 active profile 时回落到 fallback |
| V0.1-D4 | 已完成 | Keyring 删除/迁移路径未完整验收 | delete profile 时同步清理对应 `api-key:{profile_id}` keychain 槽位，legacy default key 保留 |
| V0.1-D5 | 已完成 | 诊断报告会输出 base URL/model/profile/key 状态 | Settings 已提示诊断只包含 profile/base URL/model/key saved 状态，不包含 API key 明文 |

### E. 会话、消息与学习闭环

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-E1 | 已完成 | 应用启动和切换角色会自动创建新 session 并写 greeting | 切角色只加载最近历史；无历史时不写空 session；首次发送/用户 New Chat 时才创建 session |
| V0.1-E2 | 已完成 | `addMessage` 先更新前端再写 DB，失败时 UI/DB 可能不一致 | `addMessage` 现在先写 DB，成功后再更新 UI；失败会进入发送错误路径 |
| V0.1-E3 | 已完成 | 后端 `save_message` 缺少 role/content/session 校验 | Rust command 校验 `user/assistant/system`、非空 content、session 存在 |
| V0.1-E4 | 已完成 | preset character 删除限制只在前端 | 后端也阻止删除 `emily/kenji/sophie` 内置 preset |
| V0.1-E5 | 已完成 | Character import 对 JSON/PNG 大小和字段长度缺少统一限制 | import 前限制 2 MB 文件、PNG chunk 边界、character 字段长度，错误可读 |

### F. 反馈、总结与模型输出鲁棒性

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-F1 | 已完成 | 反馈 JSON 解析依赖模型严格输出 JSON | 已增加 markdown fence、嵌入 JSON、尾逗号修复；失败时显示 raw model response fallback |
| V0.1-F2 | 已完成 | UI report 与 CLI coach-report 是两条路径 | `native-cli.md` 已说明 UI structured feedback 与 CLI Markdown coach-report 的定位差异 |
| V0.1-F3 | 已完成 | 反馈请求缺少 timeout 和错误分类 | 反馈复用 LLM timeout；前端错误提示区分 auth/network timeout/network/model/API |

### G. 前端体验与 i18n 收口

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-G1 | 已完成 | 多处硬编码 `alert/confirm` 英文 | App.vue、CharacterModal.vue 关键确认/提示已迁入 locale |
| V0.1-G2 | 已完成 | 语音错误文案在 composable 内手写中英文 | V0.1 保留本地双语函数，关键错误中英文路径一致；后续可再迁入 locale key |
| V0.1-G3 | 已完成 | 发送失败后用户消息保留但没有 retry UI | ChatPanel 已提供 Retry last，只重试 assistant 回复，不重复追加 user 消息 |
| V0.1-G4 | 已完成 | 顶部功能按钮仍有英文和原型感 | 顶部功能按钮文案已走 i18n，保留 emoji 图标 |

### H. CLI / Agent 安全边界与文档

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-H1 | 已完成 | `coach-report` 会把 transcript 发给模型，但文档提示还不够醒目 | `native-cli.md` 和 README 已明确 `--allow-network` 会发送 session transcript 到配置的模型端点 |
| V0.1-H2 | 已完成 | Agent network read-only 工具不写 audit | `native-cli.md` 已明确 network read-only coach-report 不写 DB 或 audit log |
| V0.1-H3 | 待人工验收 | CLI/Agent 真实端点只做 mock 自动化 | V0.1 人工测试至少覆盖 CLI `send-message`、`retry-last`、`coach-report` 的一个真实 OpenAI-compatible endpoint |

### I. 验证与交付

| ID | 状态 | 缺口 | 补全标准 |
| --- | --- | --- | --- |
| V0.1-I1 | 已完成 | 自动化没有 clippy/test 纳入 V0.1 验证 | `validate:v0.1` 已纳入 `cargo fmt --check`、`cargo clippy -D warnings`、`cargo test`、原 Lv.4 smoke |
| V0.1-I2 | 待人工验收 | 真实端点验收结果未写入 checklist | `lv4-validation-checklist.md` 已扩展 V0.1 验收项，等待真实端点测试结果填写 |
| V0.1-I3 | 待人工验收 | 安装包启动测试仍需人工确认 | 安装 NSIS 或 MSI 后验证启动、Settings、聊天、语音入口 |
| V0.1-I4 | 已完成 | 无已知问题清单 | README 已新增 Known V0.1 Limitations，覆盖 Ollama、Whisper、Browser STT/TTS、diagnostics 和 coach-report transcript 隐私 |

## V0.1 不做清单

以下内容不进入 V0.1，即使已经在规划里出现，也不混进这次补全:

- 新练习模式产品化: IELTS、Interview、完整 Roleplay/Drill 流程。
- Realtime/WebRTC/LiveKit 连续语音。
- 角色市场后端、账号系统、在线分发。
- 完整统计、成就、打卡系统产品化。
- Character Memory 长期记忆。
- Tauri 自动更新、托盘、全局快捷键。
- 大规模 UI/主题重做。
- Agent 自动操控 UI 或桌面全程接管。

## 执行顺序

1. 统一 V0.1 命名和验证入口。
2. 修 Rust panic/clippy/test 基线。
3. 补网络 timeout、STT/TTS 资源边界。
4. 补 Provider Profiles rename/delete/key cleanup 和配置校验。
5. 修 session 污染、message 保存一致性、后端校验。
6. 强化 feedback JSON fallback 和用户可理解错误。
7. 清理关键硬编码文案与测试指引。
8. 跑 `pnpm validate:v0.1:full`。
9. 做真实端点和安装包人工测试。

## V0.1 完成定义

全部 V0.1 必补项完成后，必须满足:

- `pnpm validate:v0.1:full` 通过。
- `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 至少有关键逻辑测试，不再是 0 tests。
- `lv4-validation-checklist.md` 有真实端点和桌面交互结论。
- 最新 MSI/NSIS 安装包可启动。
- README 写明 V0.1 已知限制。

到这里即冻结为 V0.1，不再继续扩功能。
