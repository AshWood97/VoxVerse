{
  "name": "voxverse",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "cli": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli --",
    "cli:status": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli -- status --json",
    "cli:profiles": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli -- config profiles list --json",
    "cli:characters": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli -- character list --json",
    "cli:audit": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli -- audit list --json",
    "cli:diagnostics": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli -- diagnostics run --json",
    "cli:agent:tools": "cargo run --quiet --manifest-path src-tauri/Cargo.toml --bin voxverse-cli -- agent tools list --json",
    "validate:v0.1": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-v0.1.ps1",
    "validate:v0.1:full": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-v0.1.ps1 -FullBuild",
    "validate:lv4": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-lv4.ps1",
    "validate:lv4:full": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate-lv4.ps1 -FullBuild"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-store": "^2.4.2",
    "vue": "^3.5.13",
    "vue-i18n": "^11.3.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
